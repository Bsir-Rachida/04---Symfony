{% extends 'base.html.twig' %}

{% block title %}Acteurs{% endblock %}

{% block body %}
   {% for program in actor.programs %}


  <div class="card-body">
    <h2 class="card-title">{{ loop.index }} -  {{ actor.name }}</h2>

    <a href="{{ path("program_show", { slug: program.slug }) }}" >série  - {{ program.title }}</a>
  
    
  </div>

    {% endfor %}

{% endblock %}
